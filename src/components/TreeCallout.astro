---
const { title, items = [] } = Astro.props;
---
<div class="my-6 rounded-2xl border border-stone-200 bg-white p-5">
  <h4 class="m-0 text-base font-semibold">{title}</h4>
  <ul class="mt-3 list-disc pl-5">
    {items.map((it) => <li class="mb-1">{it}</li>)}
  </ul>
</div>